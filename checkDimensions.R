humanLogicalGraph=read.csv("humanLogicalGraph.csv", header=TRUE)
print(dim(humanLogicalGraph))
mouseLogicalGraph=read.csv("mouseLogicalGraph.csv", header=TRUE)
print(dim(mouseLogicalGraph))
#Damn everything is absolutely fine here dude. No problems. Hmm why the errors then damn this graph stuff is really hard to run and 
# get meaningful results this absolutely does not scale. Does not scale at all moreover its important to understand the math behind this.
# As we have painfully understood its extremely important to understand the math and logic behind the core method you are using you just can't get around this
# especially because you are a bioinformatician. This is really freaking important dude.
